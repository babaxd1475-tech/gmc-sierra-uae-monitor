name: GMC Sierra UAE Monitor (manual only — execution moved to local WSL2)

on:
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Note
        run: echo "This workflow is kept for reference. Execution now runs on local WSL2."
